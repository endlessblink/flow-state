## 2024-05-22 - Accessible Focus Styles
**Learning:** Generic components like icon buttons should use brand-neutral or primary brand colors for focus indicators, not semantic status colors (like success green), to avoid confusion on destructive or neutral actions.
**Action:** Use `--brand-primary` and `--brand-primary-subtle` for focus rings on base components.
