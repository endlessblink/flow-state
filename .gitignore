node_modules
webdriver/node_modules/
dist

*storybook.log
storybook-static

# Test and debug artifacts
docs/testing/scripts/
docs/testing/screenshots/
testing-artifacts/screenshots/
testing-artifacts/manual-tests/
.claude/skills/docs/reports/test-artifacts/
**/test-artifacts/
**/*.webm
**/*.trace.zip

# Worktrees inside main repo (prevent accidents)
stable-working-version/

# Debug documentation
docs/debugging/
docs/guides/

# Temporary test files in root (prevent regression)
test-*.js
test-*.cjs
test-*.spec.ts
check-*.mjs

# Debug screenshots (Claude Code + Playwright debug captures)
.dev/screenshots/

# Backup files
*-backup-*.md
*.bak

# Database backups (keep locally, don't commit - contain user data)
supabase/backups/*.sql

# Log files with sensitive data (CRITICAL SECURITY)
firebase-debug.log
*.log

# Environment files - use Doppler or copy from .env.example (CRITICAL SECURITY)
.env
.env.local
.env.production
.env.*.local
.env.self-host
.env.self-host.test

# Private planning documents (keep local only, do not push to GitHub)
/plan.md

# Document-sync generated files
system_state.json
code_features_report.md
doc_verification_report.md
docs_update_plan.md
verification_results.json

# Playwright MCP screenshots
.playwright-mcp/

# Test results (generated)
test-results/
playwright-report/data/

# Debug images
docs/.debug/
.debug-images
src-tauri/*.zip
stats.html
dev-dist/

# External tools and agent directories
.agent-worktrees/
Claude-Subagent-Editor/
eslint_current_report.txt

# Backup files from migrations
**/*.backup

# Test output files
test_output*.txt
test_debug_body_*.html
group_fail_*.html
token-migration-report.json

# External widget (tracked in packages/kde-widget/)
# kde-widget/  # removed to allow tracking

# Shadow backup snapshots (user-specific data)
public/shadow-latest.json

# Large backup files (exceed GitHub 100MB limit)
backups/

# Development screenshots in project root
/*.png

# Debug artifacts (prevent root clutter)
/debug_*.txt
/eslint_*.txt
/node_debug.txt
/independence_test_log.txt
/repro_log.txt
/structure_debug.txt
/sync_debug.txt
/unused-files.txt

# Audit/report artifacts
audit-reports/
/eslint-*.json
/full-dead-code.json
/health-results.json
/pwa-report.json
.dev-server.pid

# TLS certificates (private key must never be committed)
scripts/certificates/server.key

# Claude Code local settings (may contain personal paths/preferences)
.claude/settings.local.json

# OSS safety — files containing secrets/PII removed before open-sourcing
reports/
console-task-*.txt
.claude/plans/
latest.json
Caddyfile.prod
snapshot-*.md
current-state.md
lint_report.*
components_lint_report.json
# docs/archive/ — tracked (historical plans, research, process docs)
any_files.txt
bulk_replace.js
typecheck_output.txt
# .claude-and-conquer/ — deleted

# Debug screenshots (use screenshots/ dir instead)
!public/**/*.png
!src/**/*.png
!packages/**/*.png

# Test coverage output
coverage/

# Debug screenshots directory
screenshots/

# Stale lockfiles
pnpm-lock.yaml

# Cursor AI
.cursorrules
.watchpost/

# Per-project plugin runtime state
watchpost/.claude/
watchpost/.omc/
release/
.dev-debug-electron.mjs
