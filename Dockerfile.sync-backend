FROM node:18-slim
WORKDIR /app
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
COPY scripts/sync-backend.cjs .
COPY scripts/jwks.json .
RUN npm install express pg jsonwebtoken cors
EXPOSE 3000
CMD ["node", "sync-backend.cjs"]
