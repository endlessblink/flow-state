# BUG-2071 Sure gate — v1.4.505

1. **Root cause:** v1.4.504 shipped an older sealed `dist-electron/updater.js` whose direct-replacement readiness verifier rejected valid multi-line provenance, so the installer restored the known-good AppImage and retained the failed marker.
2. **Confidence:** HIGH / PASS. The packaged-archive regression rejects that old verifier, the extracted-runtime test accepts formatted expected-version provenance while rolling back stale provenance, and the independent challenge runner passed all four acceptance items for snapshot `ae308f0ad25331bcc2d0be7dda103378e951410472a35d938035441dfd661f8e`.
3. **Candidate:** v1.4.505 seals the exact direct provenance matcher into the AppImage and blocks release when the sealed updater lacks it; the prediction is that valid expected-version replacements remain installed without weakening stale-provenance rollback or retry-marker protections.
4. **Existing protection preservation:** the installer’s rollback and failed-marker safeguards were retained. Their original harm prevention (never keep an unready replacement) is still reachable through the stale-provenance runtime test; the narrow matcher change preserves that rejection while accepting the valid formatted response that the old grep verifier falsely rejected.
5. **Regression surface:** validator fixture coverage rejects the old sealed verifier; direct installer runtime coverage tests both formatted healthy provenance and stale rollback; retry/marker coverage remains in the updater suites. Deployment, public read-back, and installed-app retry are separate remaining gates.
