# BUG-2067 gate evidence

- Candidate: stale active recurring count repair in `flowstate_done_for_now_h3_base`.
- Falsifiable prediction: a historical completion at count 4 and a different occurrence date must not block the active August 31 occurrence; completion must create September 7, preserve the historic count 4, advance the active recurrence count to 6, and retain the same-date duplicate rejection.
- Command: `scripts/db/test-reliable-assistant-contract.sh`
- Result: PASS on 2026-09-01; the disposable contract replayed the migration twice and completed the rescheduled-occurrence and same-date guard assertions.
- Protection retained: same parent/count/date completion remains `already_completed`; the existing unique completed-occurrence index remains in force.
