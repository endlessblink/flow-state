# BUG-2070 evidence

Candidate: F2 reordering consumes only canonical task-slot moves and never applies the planner's unrelated group geometry moves.

Prediction: dragging a task while F2 is held preserves the group's exact geometry while persisting the intended task order.

Proof:

- `npm run type-check` completed cleanly.
- `npm test -- --run tests/unit/canvas/tidy-layout.test.ts tests/unit/canvas/canonical-layout.test.ts` completed with 45 passing tests.
- `npx playwright test tests/e2e/canvas-geometry-local.spec.ts --config=tests/e2e/playwright.canvas-local.config.ts --workers=1 --grep "F2 drag reorders a task and persists the new slot after sync" --retries=1` completed cleanly. The scenario verifies the exact 420x900 group geometry after an explicit sync and the reordered task sequence after reload.
- `git diff --check` completed cleanly.

Preserved protection: ordinary tidy-layout flows still receive and apply group moves; only the F2 ordering path rejects them. The focused canonical-layout suite remains green.
