# TASK-2077 challenge evidence

- Candidate: preserve Board's existing local-calendar computation while starting Vitest with `Asia/Jerusalem` before its worker pool exists, and send the three reported fire-and-forget task mutations through the existing exact stale-target settlement boundary.
- Falsifiable prediction: the Board `today` repro resolves to the local date before UTC midnight; the Focus subtask toggle, Focus completion, and subtask-progress writes do not become unhandled rejections when the task disappeared; unrelated mutation failures still reject.
- Red proof: `npm test -- tests/contract/task-background-mutation-contract.test.ts src/composables/board/__tests__/useBoardActions.spec.ts --run` failed before the three callers were routed through the settlement helper.
- Focused green proof: `npm test -- tests/contract/task-background-mutation-contract.test.ts tests/unit/task-mutation-error-handling.test.ts src/composables/board/__tests__/useBoardActions.spec.ts --run` passed 3 files and 7 tests.
- Full regression proof: `npm test -- --run` passed 409 files on 2026-09-04, including the Board local-date test.
- Type proof: `npm run type-check` exited 0 on 2026-09-04.
- Lint proof: `npx eslint src/views/FocusView.vue src/composables/tasks/useTaskEditActions.ts` reported no errors; it retained one existing non-null-assertion warning in `useTaskEditActions.ts`.
- Protection retained: `settleBackgroundTaskMutation` suppresses only errors beginning `Task update target no longer exists:`. Its existing unit coverage proves unknown errors reject, so the global error path remains available for real failures.
- Release note: `npm run electron:build` reached the unrelated backup/restore browser gate and stopped because its seeded task was absent from the test database. No package or updater artifact is claimed by this evidence.
