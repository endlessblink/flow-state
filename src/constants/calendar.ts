/** Height in px of each half-hour slot in calendar views */
export const CALENDAR_SLOT_HEIGHT_PX = 30

/** Snap grid resolution in minutes for calendar drag operations */
export const CALENDAR_SNAP_MINUTES = 15

/** Maximum slot index (48 half-hour slots per day, 0-based) */
export const CALENDAR_MAX_SLOT_INDEX = 47
